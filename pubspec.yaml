name: flutter_practice
description: Learn flutter component

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.0
  http:
#  map_view:
  location:
  share:
  firebase_database:
  cloud_firestore:
#  firebase_storage:
  image_picker:
  connectivity:
#  carousel: ^0.0.1
  sqflite: any
  path_provider:
  rxdart: ^0.16.1
  charts_flutter: ^0.4.0
  map_view: 0.0.14

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/company_logo.png
    - assets/no_internet.png
    - assets/developer_background.jpg
    - assets/android_frame.png
    - assets/resources/lang/tr.json
    - assets/resources/lang/en.json
    - assets/resources/lang/ja.json

